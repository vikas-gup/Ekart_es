<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/items">Ekart</a>
    </div>
    <ul class="nav navbar-nav">
      <li><%= link_to "Category", '/categories' %></li>
      <li><%= link_to "Items", '/items' %></li>

      <%= form_tag(items_path, method: :get) do %>
        <%= text_field_tag :search_item, params[:search], class: "form-control" %>
        <%= submit_tag 'Search', name: nil %>
      <% end %>

    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li> <%= link_to "My Cart", "/carts/#{$current_user.id}" %> <span class="glyphicon glyphicon-user"></span> </li>
      <li> <%= link_to "My Orders", '/orders' %> <span class="glyphicon glyphicon-user"></span> </li>
      <li> <%= "Hello #{$current_user.firstname}" %> </li>
    </ul>
  </div>
</nav>